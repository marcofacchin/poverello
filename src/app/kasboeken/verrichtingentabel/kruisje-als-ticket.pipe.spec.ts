import { KruisjeAlsTicketPipe } from './kruisje-als-ticket.pipe';

describe('KruisjeAlsTicketPipe', () => {
  it('create an instance', () => {
    const pipe = new KruisjeAlsTicketPipe();
    expect(pipe).toBeTruthy();
  });
});
