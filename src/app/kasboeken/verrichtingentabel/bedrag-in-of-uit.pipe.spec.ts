import { BedragInOfUitPipe } from './bedrag-in-of-uit.pipe';

describe('BedragInOfUitPipe', () => {
  it('create an instance', () => {
    const pipe = new BedragInOfUitPipe();
    expect(pipe).toBeTruthy();
  });
});
