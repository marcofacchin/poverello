import { LeegAlsBlancoTypePipe } from './leeg-als-blanco-type.pipe';

describe('LeegAlsBlancoTypePipe', () => {
  it('create an instance', () => {
    const pipe = new LeegAlsBlancoTypePipe();
    expect(pipe).toBeTruthy();
  });
});
